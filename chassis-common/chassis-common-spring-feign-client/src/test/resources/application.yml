spring:
  cloud:
    discovery:
      client:
        simple:
          instances:
            github:
              - service-id: github-1
                uri: https://api.github.com

logging:
  level:
    io.vzhidu.chassis.common.scof: DEBUG
